# Runtime dependencies for the Vercel Python function (api/index.py).
# Mirrors backend/pyproject.toml minus uvicorn: Vercel supplies the ASGI server.
fastapi>=0.115
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
PyJWT[crypto]>=2.8
